<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.Controls"
             xmlns:validation="clr-namespace:View.ViewModel.Validation"
             xmlns:viewModel="clr-namespace:View.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="3,0,0,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="IsReadOnly" Value="{Binding IsReadOnly}"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Foreground" Value="Red"/>
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, 
                                    Path=(Validation.Errors)/ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <viewModel:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="IsApplyButton" TargetType="Button">
            <Setter Property="Visibility" Value="{Binding IsVisibleApply,
         Converter={StaticResource BoolToVisibilityConverter}}"/>
            <Setter Property="Margin" Value="3,50,3,3"/>
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="25"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Image Grid.Column="0"
                   Grid.Row="1"
                   Width="100"
                   Height="100" 
                   Margin="3,0,3,3" 
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Source="/Resources/photo_placeholder_100x100.png"/>

            <TextBlock Text="Name:" 
               Margin="3"
               Grid.Row="0"
               Grid.Column="1">
            </TextBlock>

            <Grid Grid.Column="1" 
                  Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0"
                    Style="{StaticResource TextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path ="CurrentContact.Fullname" 
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:FullnameValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <TextBlock Grid.Row="1" 
                   Text="Phone Number:" 
                   Margin="3">
                </TextBlock>

                <TextBox Grid.Row="2"
                 Style="{StaticResource TextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path ="CurrentContact.PhoneNumber" 
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:PhoneNumberValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <TextBlock Grid.Row="3"
                   Text="Email:" 
                   Margin="3">
                </TextBlock>

                <TextBox Grid.Row="4" 
                 Style="{StaticResource TextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path ="CurrentContact.Email" 
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:EmailValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <Button Grid.Row="5"
                        Content="Apply" 
                        Command="{Binding ApplyContactCommand}"
                        Style="{StaticResource IsApplyButton}"
                        HorizontalAlignment="Left"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
