<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        xmlns:validation="clr-namespace:View.ViewModel.Validation"
        Title="Contact" Height="400" Width="400" MinHeight="250" MinWidth="350"
        Icon="pack://application:,,,/Resources/ContactsApp_96x96.ico">
    <Window.Resources>
        <Style x:Key="ValidationTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="3"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Foreground" Value="Red"/>
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, 
                        Path=(Validation.Errors)/ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ReadTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="IsReadOnly" Value="true"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="EditGroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,3,3"/>
        </Style>

        <Style x:Key="ReadGroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="3,0,0,3"/>
        </Style>

        <Style x:Key="ButtonsStackPanelStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
        </Style>

        <Style x:Key="ButtonsStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="75"/>
        </Style>

    </Window.Resources>
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header ="Edit Contact" 
                  Grid.Row="0" 
                  Grid.Column="0"
                  Style="{StaticResource EditGroupBoxStyle}">
            <StackPanel>
                <TextBlock Text="Name:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ValidationTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path ="CurrentContact.Fullname" 
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:FullnameValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <TextBlock Text="Phone Number:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ValidationTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path="CurrentContact.PhoneNumber" 
                         UpdateSourceTrigger="PropertyChanged"
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:PhoneNumberValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <TextBlock Text="Email:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ValidationTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path="CurrentContact.Email"
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="TwoWay">
                            <Binding.ValidationRules>
                                <validation:EmailValidationRule/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Read Contact" 
                  Grid.Row="0" 
                  Grid.Column="1" 
                  Style="{StaticResource ReadGroupBoxStyle}">
            <StackPanel>
                <TextBlock Text="Name:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ReadTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path="CurrentContact.Fullname"
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="OneWay">
                        </Binding>
                    </TextBox.Text>
                </TextBox> 

                <TextBlock Text="Phone Number:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ReadTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path="CurrentContact.PhoneNumber"
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="OneWay"> 
                        </Binding>
                    </TextBox.Text>
                </TextBox> 

                <TextBlock Text="Email:" Margin="3"></TextBlock>
                <TextBox Style="{StaticResource ReadTextBoxStyle}">
                    <TextBox.Text>
                        <Binding Path="CurrentContact.Email"
                         UpdateSourceTrigger="PropertyChanged" 
                         Mode="OneWay">
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" 
                    Grid.Column="1" 
                    Style="{StaticResource ButtonsStackPanelStyle}">
            <Button Content="Save" 
                    Command="{Binding SaveCommand}" 
                    Style="{StaticResource ButtonsStyle}"
                    Margin="0,0,3,0"/>
            <Button Content="Load" 
                    Command="{Binding LoadCommand}"
                    Style="{StaticResource ButtonsStyle}"
                    Margin="3,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
